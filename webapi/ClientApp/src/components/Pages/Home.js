import { Box, Typography } from "@mui/material";
import React from "react";

const Home = () => {
  return (
    <Box flex={5}>
      <Typography variant="h6">Home</Typography>
    </Box>
  );
};

export default Home;
